# Diario de decisiones técnicas

Registro breve de decisiones no triviales y su motivo, para sostener la memoria y la defensa oral del TFM.

## 2026-07-28 — Scaffolding inicial del repo

- Layout `src/seip/` (src-layout) en vez de paquete plano en la raíz, para evitar imports accidentales del código sin instalar y facilitar el empaquetado como wheel.
- `pyproject.toml` con dependencias opcionales separadas por grupo (`streaming`, `spark`, `ml`) y `requests` como única dependencia base. Motivo: la lógica de negocio pura (parseo de API, transformaciones) debe poder testearse con `pip install -e ".[dev]"` sin instalar PySpark/Databricks/MLflow, para que la suite de tests sea rápida y no dependa de un cluster.
- Se pospone la creación de `.github/workflows/batch-ingestion.yml` hasta que exista un job de batch real que ejecutar (Fase 2/3) — crear el workflow antes sería un placeholder sin valor.

## 2026-07-30 — Kafka producer (Fase 2)

- **Kafka local en Docker (KRaft, sin Zookeeper) en vez de Confluent Cloud desde el principio.** Motivo: evita depender de que el free tier de Confluent esté despierto mientras se itera, y no consume la cuota gratuita. El producer lee `KAFKA_BOOTSTRAP_SERVERS`/`KAFKA_SECURITY_PROTOCOL` de variables de entorno, así que apuntar a Confluent Cloud más adelante es solo cambiar config, no código.
- **Serialización JSON plano, no Avro + Schema Registry.** El spec no especifica formato — fue una decisión abierta. Se descartó Avro porque su valor principal (forzar contratos de esquema entre equipos independientes que no se coordinan) no aplica aquí: un único autor escribe el producer y el consumidor (job Bronze) en el mismo repo. Añadir Schema Registry habría sido una pieza de infraestructura más sin resolver un problema real del proyecto, justo el riesgo de "colección de tecnologías" que señalaron los tutores.
- **Ventana de fetch con solapamiento (últimos ~25 min) en vez de cursor exacto por indicador.** Más simple y tolera reinicios del producer sin perder datos, a costa de duplicados esperados en Bronze — se resuelven en la deduplicación de Silver por `(indicator_id, datetime_utc)`, ya prevista en el spec.
- **Un solo proceso con un loop de 5 min** (la granularidad más fina, eólica/solar) en vez de 4 procesos o un scheduler dedicado — cada indicador se publica solo cuando su intervalo nativo lo exige (PVPC cada hora, SPOT cada 15 min).
- Hallazgo de la prueba real: la API de ESIOS **sí acepta `start_date`/`end_date` con hora** (no solo fecha), lo cual no estaba confirmado — sin esto la ventana de polling habría tenido que traer el día completo en cada tick.
- Verificado end-to-end contra ESIOS real + Kafka local (`scripts/smoke_kafka_producer.py`): un tick produce y se puede consumir correctamente en los 4 topics. Pendiente: soak test de 48h (criterio de aceptación de la Fase 2) y prueba contra Confluent Cloud real.

## 2026-07-30 — REData client

- Mismo patrón que `esios_client.py` (retry/backoff, tests con mocks + smoke test real), pero sin filtro de `geo_id` — REData no mezcla países en la misma respuesta como sí hace ESIOS con el indicador SPOT.
- Parser dedicado para la forma anidada de `balance-electrico` (`attributes.content[].attributes.values` en vez de `attributes.values` directo) — verificado contra datos reales (41 valores, múltiples títulos).
- `intercambios` se deja como endpoint no crítico: se probó contra la API real y falló tras 3 reintentos (500/503), tal y como el spec advertía. No se trata como error bloqueante en el diseño del futuro `batch_job.py`.
- No se ha extraído la lógica de retry compartida entre `esios_client.py` y `redata_client.py` (queda algo duplicada) — se revisará si aparece un tercer cliente con la misma necesidad; no se ha hecho ahora para no refactorizar código ya mergeado sin necesidad concreta.

## 2026-07-30 — batch_job.py y hallazgo de entorno: PySpark local requiere JDK 11 en esta máquina

- Bronze definido como "raw JSON + metadatos de ingesta" (`ingestion_date`, `source`, `fetched_at`, `raw_json`), sin tipar — coherente con la definición de Bronze del spec (schema-on-read) y con lo ya hecho en streaming (los mensajes de Kafka tampoco se tipan, solo se envuelven). El tipado/casteo se deja para Silver.
- `intercambios` (Francia/Portugal/Marruecos) se trata como fuente no crítica dentro del batch: si falla, se loguea y se continúa — falló efectivamente en la prueba real, tal y como el spec advertía.
- **Hallazgo de entorno (no del proyecto):** PySpark no arrancaba en esta máquina con JDK 17 (Eclipse Adoptium) ni JDK 21 — la JVM fallaba al crear una pipe de loopback interna vía Unix Domain Sockets (`SocketException: Invalid argument: connect` en `sun.nio.ch.UnixDomainSockets`), algo que las builds recientes de esos JDKs usan también en Windows. Se resolvió instalando **Eclipse Temurin JDK 11** (`C:\java-tools\jdk-11.0.32+9`, zip portable, sin instalador) y apuntando `JAVA_HOME` ahí — JDK 11 no usa esa optimización y conecta por TCP loopback normal. Cualquier trabajo con PySpark local en esta máquina debe usar ese JDK 11 vía `JAVA_HOME`, no el JDK 17/21 del sistema.
- Verificado end-to-end con `scripts/smoke_batch_job.py`: 75 filas escritas y leídas correctamente en una tabla Delta local particionada por `ingestion_date` (30 generación + 4 renovable/no-renovable + 41 balance).
- Se extrajo el bootstrap de la SparkSession local a `seip/ingestion/spark_session.py` (`build_local_spark_session`) para no duplicarlo cuando `streaming_bronze.py` lo necesitó también.

## 2026-07-30 — streaming_bronze.py (cierra el hueco de Bronze streaming)

- Job de Spark Structured Streaming que consume los 4 topics de Kafka y aterriza en `data/bronze/esios` — sin esto, el producer solo escribía a Kafka pero nada materializaba esos datos en Bronze de forma inmutable (Kafka por sí solo no cumple la definición de Bronze del spec al tener retención limitada). Es el equivalente streaming de `batch_job.py`, con el mismo esquema de fila (`ingestion_date`/`source`/`fetched_at`/`raw_json`) pero en tabla Delta separada, al ser una fuente estructuralmente distinta (eventos ESIOS de alta frecuencia vs. series diarias REData).
- Trigger `availableNow` (procesa lo disponible y para) en vez de continuo/siempre encendido — coherente con el presupuesto de Azure limitado; se programaría cada 15-30 min vía un Job de Databricks en vez de mantener un cluster streaming 24/7. Queda pendiente probar el modo continuo si se quiere algo más "tiempo real" para el vídeo de demo.
- **Bug real encontrado y corregido durante la verificación:** la columna `timestamp` que da el conector de Kafka en Spark llega como datetime *naive*, interpretada en la zona horaria por defecto de la JVM, no en UTC — el primer `fetched_at` de streaming salía sin offset y con una hora distinta al de batch. Se corrigió fijando `spark.sql.session.timeZone=UTC` en `build_local_spark_session` y además adjuntando explícitamente `tzinfo=UTC` en `to_bronze_row` (doble seguro: el valor es correcto aunque alguien construya la sesión sin ese config). Sin este arreglo, cualquier análisis posterior en Silver que cruzara `fetched_at` de streaming con el de batch habría tenido un desfase horario silencioso.
- Verificado end-to-end con Kafka local + PySpark (`scripts/smoke_streaming_bronze.py`): 25 filas correctas repartidas en los 4 topics, coincidiendo exactamente con los mensajes producidos en las pruebas de `kafka_producer.py`.

## 2026-07-30 — bronze_to_silver.py (solo ESIOS streaming)

- **Alcance de este PR limitado a Silver para el Bronze streaming ESIOS** (PVPC/SPOT/eólica/solar), acordado explícitamente para no mezclar en un mismo cambio "tipar y limpiar" con "unir series de granularidades distintas". Silver de REData batch y el join temporal hora a hora quedan como PRs separados.
- **La lógica de parseo/validación existe dos veces a propósito:** una versión en Python plano (`parse_bronze_record`/`validate_record`), rápida de testear sin Spark, que documenta la regla; y una versión en expresiones nativas de Spark (`parse_bronze_stream`/`add_validation_flags`), usada de verdad en `run`. Motivo: `dropDuplicates` + `withWatermark` solo funcionan como operadores de estado genuinos de streaming si se aplican sobre columnas nativas de Spark en un DataFrame de streaming real — si se hiciera el parseo fila a fila en Python dentro de un `foreachBatch` (como en `batch_job.py`/`streaming_bronze.py`), el dedup solo actuaría dentro de cada micro-batch, no across ejecuciones, que es justo lo que exige la Regla de dedup del spec.
- Solo se implementan las dos reglas de rango que el spec da explícitamente (PVPC/SPOT 0-700€, solar nocturna >10MW) — el "etc." del spec se deja como TODO explícito en vez de inventar reglas no especificadas.
- Los valores fuera de rango se **marcan** (`validation_flags`, array de strings) en vez de descartarse — Silver conserva toda la fila para que Gold/análisis decida qué hacer con datos flageados, coherente con "alerta" del spec (no "elimina").
- Bronze se lee como **stream** (no como batch estático) específicamente para que el watermark de 2h tenga sentido — un read batch no tiene noción de watermark. El trigger sigue siendo `availableNow`, igual que en `streaming_bronze.py`, así que esto no obliga a mantener un cluster encendido 24/7.
- **Verificado de verdad, no solo con mocks:** se inyectó manualmente un duplicado real en Bronze (mismo `indicator_id`+`datetime_utc`, dos filas) con un valor fuera de rango (999€) para comprobar en una sola prueba tanto el dedup como el flag de validación. Resultado: 40 filas en Bronze → 39 en Silver, la fila superviviente correctamente marcada `value_out_of_range_0.0_700.0`.
- **Rareza encontrada y no completamente explicada:** en una ejecución incremental (sin limpiar el checkpoint), una fila de Bronze añadida por un proceso Spark distinto al pipeline habitual no fue recogida por esa pasada — sí se recogió correctamente al limpiar el checkpoint de Silver y rehacer una pasada completa desde cero. No se ha investigado más a fondo porque no debería darse en el pipeline real (todo Bronze se escribe siempre desde los mismos jobs programados, nunca desde un proceso ad-hoc como el usado para esta prueba).

## 2026-07-30 — bronze_to_silver.py: Silver para REData batch

- **Renombradas las funciones ESIOS con sufijo `_esios`** (`parse_bronze_record` → `parse_esios_bronze_record`, `validate_record` → `validate_esios_record`, `parse_bronze_stream` → `parse_esios_bronze_stream`, `add_validation_flags` → `add_esios_validation_flags`, `run` → `run_esios`) al añadir el segundo pipeline al mismo fichero — con dos fuentes en un archivo, nombres genéricos como `run()` habrían sido ambiguos. Es un rename de código ya mergeado, hecho deliberadamente ahora que la necesidad de distinguir dos pipelines es real, no antes.
- **`run_redata` es batch simple (lectura estática + `overwrite`), no streaming** — a diferencia de ESIOS, REData se ingesta vía un job batch programado (`batch_job.py`), no hay llegadas tardías de un stream que gestionar, así que el watermarking del spec (que es explícitamente "para llegadas tardías en streaming") no aplica aquí. Evita la complejidad de Structured Streaming donde no aporta nada.
- **UTC normalizado a partir del campo `datetime` con offset** (p.ej. `+02:00`), no de un `datetime_utc` ya calculado — REData, a diferencia de ESIOS, no proporciona ese campo ya resuelto.
- **Clave de dedup `(source, title, datetime_utc)`** en vez de `(indicator_id, datetime_utc)` — REData no tiene `indicator_id`; `source` (el endpoint/task) + `title` (la serie concreta dentro de ese endpoint, p.ej. "Eólica" o "Renovable") identifican la serie.
- **Sin reglas de validación de rango para REData** — el spec no da ninguna regla explícita para esta fuente (a diferencia de PVPC/solar), y se ha comprobado que asumir "value >= 0" sería incorrecto: datos reales de `generacion_estructura` ya muestran valores negativos legítimos (p.ej. "Carbón": -3). Inventar una regla sin base habría sido peor que no tener ninguna.
- Verificado end-to-end (`scripts/smoke_bronze_to_silver_redata.py`) contra datos reales: 75 filas Bronze → 75 Silver sin duplicados naturales; **dedup confirmado inyectando un duplicado real** (77 → 76, colapsa correctamente). Esta vez no apareció la rareza de checkpoint del PR anterior — coherente con que aquí no hay checkpoint de streaming, es una sobrescritura batch completa cada vez.

## 2026-07-30 — bronze_to_silver.py: join temporal hora a hora (ESIOS)

- **Agregación por media dentro de la hora** para cada serie: PVPC ya es horario (promediar una sola lectura es un no-op), SPOT (15min) y eólica/solar (5min) se promedian de verdad a nivel hora. Es la interpretación más directa de "join temporal ... agregados a hora" del spec.
- **Pivot a tabla ancha** (una fila por hora, columnas `pvpc_eur_mwh`/`spot_eur_mwh`/`eolica_mw`/`solar_mw`) en vez de dejarlo en formato largo — así queda directamente lista para la feature store de ML (lags de precio, % renovable de la última hora, etc. de la sección 6 del spec), sin que Gold tenga que repetir el join.
- **Horas con algún indicador ausente se conservan con `NULL`** en esa columna en vez de descartarse — misma filosofía que las validation_flags: Silver no oculta huecos, Gold/ML decide cómo tratarlos (p.ej. imputación o descarte en el feature engineering).
- Lee el **Silver ya deduplicado de `run_esios`**, no el Bronze crudo — este paso es un batch simple (lectura + `overwrite`), sin necesidad de streaming/watermark propio, porque esa responsabilidad ya la resolvió `run_esios` antes.
- Igual que en las piezas anteriores, la lógica de agregación/pivot existe también en Python plano (`aggregate_records_to_hour`/`pivot_hourly_records`) como referencia rápida de testear, con la versión nativa de Spark (`aggregate_esios_silver_to_hour`/`pivot_hourly_indicators`) haciendo el trabajo real.
- Verificado end-to-end (`scripts/smoke_bronze_to_silver_hourly_join.py`) contra datos reales: 4 filas horarias, con SPOT/eólica/solar promediados correctamente dentro de cada hora y `NULL` correcto en las horas con datos parciales (incluyendo el registro de prueba fuera de rango inyectado en el PR anterior, que aparece aislado en su propia hora sin el resto de indicadores).

## 2026-07-30 — silver_to_gold.py: 2 de los 3 KPIs (solo ESIOS)

- **Alcance limitado a los dos KPIs que solo necesitan el Silver horario de ESIOS** (`data/silver/esios_hourly`): precio medio por hora del día, y comparativa PVPC vs SPOT. El tercer KPI del spec, `% penetración renovable`, necesita la generación total de REData (granularidad día) cruzada con el Silver de ESIOS (granularidad hora) — una decisión de diseño real (p.ej. repartir el % diario a cada hora de ese día) que se deja para un PR aparte en vez de improvisarla aquí.
- **Los valores ausentes se saltan al promediar, no se tratan como 0** — si un día falta el PVPC de una hora concreta, esa hora no debe salir artificialmente más baja. Verificado explícitamente con un test unitario (`test_average_by_hour_of_day_skips_missing_values_instead_of_treating_as_zero`) porque es el tipo de bug fácil de colar sin darse cuenta.
- `spread_eur_mwh` (PVPC - SPOT) usa la resta nativa de Spark, que ya propaga `null` automáticamente si falta cualquiera de los dos operandos — a diferencia de la versión Python de referencia (`compute_spread`), que sí necesita el chequeo explícito de `None`.
- Ambos KPIs son batch simple (lectura del Silver horario + `overwrite`), sin streaming/watermark propio — esa responsabilidad ya la resolvió `run_esios` antes de llegar aquí.
- Verificado end-to-end (`scripts/smoke_silver_to_gold.py`) contra datos reales: `NULL` correcto donde falta un precio, `spread_eur_mwh` bien calculado donde hay ambos. Con los datos de prueba actuales (pocas horas de un único día) no se llega a ejercitar el promedio real entre varios días para una misma hora — eso queda cubierto por los tests unitarios en Python puro, no por este smoke test.

## 2026-07-30 — Corrección importante: bug de zona horaria en `streaming_bronze.py` (el arreglo anterior estaba incompleto)

Encontrado mientras se verificaba `features.py` con un cross-check Spark-vs-Python: los resultados salían sistemáticamente desplazados 2 horas. Investigado a fondo con una prueba aislada (`CAST('2026-07-30 12:00:00' AS TIMESTAMP)` → `.show()` da la hora correcta, pero `.collect()` la devuelve como "14:00:00").

- **Causa raíz real:** `pyspark`, al hacer `.collect()` de una columna `TimestampType`, convierte el valor a `datetime` de Python usando la **zona horaria por defecto de la JVM**, ignorando por completo `spark.sql.session.timeZone`. Esto es distinto de `.show()`/expresiones SQL, que sí respetan esa config — la distinción no es obvia y no está bien documentada.
- **Esto invalida parcialmente el arreglo del PR de `streaming_bronze.py`** (el que fijaba `spark.sql.session.timeZone=UTC` y luego hacía `.replace(tzinfo=utc)` sobre el valor recogido): ese `.replace` estaba etiquetando como UTC un valor que en realidad seguía en hora local de la JVM (CEST, +2h en verano). El `fetched_at` de Bronze streaming llevaba hasta 2 horas de más desde que se "arregló" mal.
- **Alcance real del error, para no sobredimensionarlo:** el dato de negocio (`datetime_utc`, `value`, etc. dentro de `raw_json`) **nunca estuvo mal** — viene de parsear el JSON de la API como string en Python, no de recoger un `TimestampType` de Spark. Lo único afectado era el metadato de ingesta `fetched_at`. El efecto real más relevante: el watermark de `run_esios` (`withWatermark("fetched_at", "2 hours")`) usa ese campo, así que su noción de "cuánto margen dar a llegadas tardías" estaba operando sobre una marca de tiempo desplazada.
- **Arreglo correcto:** castear la columna a `double` (epoch, segundos desde 1970 — no depende de ninguna zona horaria) **antes** de hacer `.collect()`, y reconstruir en Python con `datetime.fromtimestamp(epoch, tz=timezone.utc)`. Verificado de forma aislada y determinista contra un valor conocido.
- El mismo patrón (recoger un `TimestampType` con `.collect()` sin pasar por epoch) apareció también en el propio script `scripts/smoke_features.py` (al recoger `hour_utc` para el cross-check) — no era un bug de producción (el pipeline real de `features.py` nunca saca timestamps a Python), pero confirma que es un error fácil de cometer sin darse cuenta; corregido igual con el cast a epoch.
- **Regla general de aquí en adelante para todo el proyecto:** nunca hacer `.collect()` de una columna `TimestampType` de Spark directamente — castear siempre a epoch (`double`/`long`) primero y reconstruir el `datetime` en Python con `tz=timezone.utc` explícito.
- Rehecha toda la cadena downstream desde cero con los datos corregidos (Bronze streaming → Silver ESIOS → join horario → Gold KPIs → features) y reverificado end-to-end contra datos reales; el cross-check Spark-vs-Python de `features.py` ahora da "all rows match exactly".

## 2026-08-05 — Backfill histórico + train.py: modelo real entrenado y validado

- **Antes de entrenar, se decidió explícitamente hacer un backfill histórico real** en vez de conformarse con datos sintéticos o con las pocas horas de prueba acumuladas — el criterio de aceptación de la Fase 4 exige un test de ≥3 meses, y con horas no había ni para el lag de 168h. Decisión tomada junto con el usuario antes de escribir código.
- `historical_backfill.py` reutiliza `kafka_producer.INDICATORS` (misma tupla indicator_id/topic/geo_id que usa el streaming) como única fuente de verdad de qué 4 indicadores se ingieren — evita tener esa lista definida en un tercer sitio. Aterriza en el mismo `data/bronze/esios` que el streaming, con el mismo esquema de fila, así que Silver/join/features no necesitan saber si un registro vino de Kafka o del backfill.
- Paginado por mes calendario con pausa de cortesía de 1.2s (Regla 3). Un fallo en un mes de un indicador se loguea y se salta, no aborta todo el backfill — perder un mes de un indicador en una ejecución de horas es mucho mejor que perderlo todo por un fallo transitorio.
- **Ejecutado de verdad:** 6 meses (2026-02-06 a 2026-08-05), 4 indicadores → 125.279 filas en Bronze. Verificado end-to-end por toda la cadena: Silver (125.255, tras el dedup natural), join horario (4.320 = 180 días × 24h exacto, sin huecos), features (4.320, cross-check Spark-vs-Python "all rows match exactly" también a esta escala).
- **`train.py`: modelos independientes por horizonte (h+1 a h+24), no forecasting recursivo** — la elección explícita del spec para evitar acumulación de error.
- **Las variables cíclicas del target se recalculan para la hora objetivo (t+h), no se reutilizan las de la hora de origen (t).** Es una decisión de diseño con impacto real: para h=24 coinciden (mismo hora del día 24h después), pero para h=1..23 no — usar las de origen habría hecho perder al modelo la señal del ciclo diario para la mayoría de horizontes. `build_horizon_training_set` recalcula hour_sin/cos/dow_sin/cos/month sobre `hour_utc + INTERVAL h HOURS`, no sobre `hour_utc`.
- **Split train/test cronológico, nunca aleatorio** — un split aleatorio en series temporales filtraría información del futuro cercano al training a través de filas vecinas.
- Baseline: persistencia naive (`pvpc_lag_24h`, el precio de la misma hora el día anterior) — exactamente el baseline que pide el spec.
- Se registra en el Model Registry **solo el modelo de h+1** como versión de referencia (alias `reference`), no los 24 — el criterio de aceptación solo exige "al menos una versión marcada como referencia", y h+1 es el horizonte más directamente accionable para representar.
- **Bug encontrado y corregido en el camino:** `mlflow.sklearn.log_model` falla al guardar un `LGBMRegressor` en MLflow 3.x — su serialización pasa por `skops`, cuya lista de tipos de confianza por defecto no incluye `lightgbm.basic.Booster`/`lightgbm.sklearn.LGBMRegressor`, aunque `LGBMRegressor` implemente la interfaz de estimador de sklearn. Arreglado usando `mlflow.lightgbm.log_model` (el flavor nativo de LightGBM), que no pasa por esa comprobación.
- **Resultado real, no simulado:** entrenado y evaluado con `scripts/smoke_train.py` contra los datos reales del backfill (test = últimos 3 meses). **Los 24/24 horizontes superan el baseline naive en RMSE y MAE** — p.ej. h+1: RMSE 29.43€ vs 55.28€ del baseline (~47% mejor); h+24: RMSE 32.70€ vs 62.76€ (~48% mejor). Cumple el criterio de aceptación de la Fase 4 tal cual está escrito en el spec. Verificado el registro: `seip-pvpc-forecast-h1` v1 con alias `reference` confirmado vía `MlflowClient`.
- MLflow 3.x crea `mlflow.db` (SQLite) localmente además de `mlruns/` — añadido a `.gitignore` junto con `mlartifacts/`.

## 2026-08-05 — Revisión: registrar los 24 modelos, no solo h+1

Al diseñar `inference.py` (que debe generar las 24 predicciones siguientes según el spec) quedó claro que registrar solo h+1 no era suficiente — aunque cumplía la letra del criterio de aceptación ("al menos una versión de referencia"), `inference.py` necesita el modelo de referencia de **cada** horizonte para producir el forecast completo de 24h.

- `run()` ahora registra los 24 modelos, cada uno con su propio nombre (`seip-pvpc-forecast-h{h}`) y su propio alias `reference` — cada horizonte es un problema distinto (dataset de entrenamiento distinto, perfil de error distinto), así que "la versión de referencia" tiene que existir por horizonte, no una sola compartida.
- Reentrenado y confirmado: 24/24 registrados correctamente (`h1` como v2 por ser reentreno, `h2`-`h24` como v1), resultados RMSE/MAE idénticos a la corrida anterior.

## 2026-08-05 — inference.py

- **`compute_target_calendar_features` se movió de estar solo-inline-en-Spark dentro de `build_horizon_training_set` a ser también una función Python pura en `train.py`**, reutilizada tal cual por `inference.py`. Antes de este cambio, escribir `inference.py` habría significado reimplementar esa lógica de cero una segunda vez (con riesgo real de que las dos versiones divergieran silenciosamente); ahora hay una sola fuente de verdad entre entrenamiento e inferencia.
- **Antes de escribir `inference.py`, se verificó de forma aislada si `toPandas()` sufre el mismo bug de zona horaria que `.collect()`** (encontrado en `streaming_bronze.py`) — no lo sufre: `toPandas()` respeta `spark.sql.session.timeZone` correctamente (confirmado con `CAST('2026-07-30 12:00:00' AS TIMESTAMP)` → `toPandas()` da la hora correcta). Esto confirma que `train.py`, que ya usaba `toPandas()` extensamente para pasar a pandas/LightGBM, nunca tuvo ese bug — solo lo tenía el `.collect()` fila a fila de `streaming_bronze.py`.
- **Las 24 predicciones se calculan siempre desde el mismo "origin" (la última fila de features disponible)** — así las 24 salidas son un único forecast coherente hecho "ahora", no 24 predicciones puntuales sin relación entre sí.
- Verificado end-to-end (`scripts/smoke_inference.py`) contra los 24 modelos reales ya registrados: 24 predicciones con un patrón diario que tiene sentido físico real (precio bajo a mediodía por generación solar alta, pico en la punta de demanda de tarde-noche) — buena señal cualitativa adicional a las métricas RMSE/MAE de que el modelo capturó estructura real del precio, no ruido.

## 2026-08-05 — % penetración renovable (KPI + feature)

- **REData ya calcula el `percentage` diario de "Renovable"** (Renovable/(Renovable+No renovable), confirmado contra datos reales: 0.4813 + 0.5187 = 1.0) — no hace falta recalcularlo, solo repartirlo a las 24 horas del día que cubre.
- **El reparto se hace con un join por rango en el instante UTC** (`hour_utc >= day_start_utc AND hour_utc < day_start_utc + 24h`), no comparando fecha de calendario UTC. Motivo, encontrado al revisar los datos reales antes de escribir código: el `datetime_utc` diario de REData marca el inicio de ese día **en hora española**, que en verano (CEST, UTC+2) cae a las 22:00 UTC del día anterior — comparar por `date(hour_utc) == date(datetime_utc)` habría asignado el % correctamente solo a 2 de las 24 horas. Verificado con datos reales: el corte cae exactamente a las 22:00 UTC como se esperaba.
- **`renewable_share` es la mezcla renovable diaria completa de REData** (incluye hidráulica y otras renovables, no solo solar+eólica), repartida sin cambios a cada hora de ese día — una aproximación razonable dado que REData no publica este dato a nivel hora, no una cifra horaria exacta de solar+eólica. Documentado explícitamente para no dar una imagen más precisa de la que es.
- La lógica del join (`broadcast_daily_renewable_share`) vive en `silver_to_gold.py` y se reutiliza tal cual desde `features.py` (`add_renewable_share_feature`) — un único sitio para el cruce REData-día/ESIOS-hora, usado tanto por el KPI de Gold como por la feature de ML.
- **`renewable_share` no está todavía en `FEATURE_COLUMNS` de `train.py`** — añadirla implicaría reentrenar los 24 modelos ya registrados, y se ha dejado como decisión explícita pendiente en vez de hacerlo de forma retroactiva sin comentarlo.
- **Limitación real encontrada al verificar contra datos reales:** el Silver de REData solo cubre 2 días (nunca se hizo un backfill histórico de REData como el de ESIOS), así que de las 4.320 horas del feature store, solo 48 (2 días × 24h) tienen `renewable_share` asignado. El join está verificado como correcto sobre esos datos; la cobertura completa depende de decidir si se justifica un backfill histórico de REData, dado que es una pieza de segunda prioridad frente al núcleo ya cerrado.

## 2026-08-05 — Backfill histórico de REData (cierra la cobertura de `% renovable`)

- **Más simple que el backfill de ESIOS, y en el mismo fichero (`batch_job.py`), no uno nuevo.** REData en granularidad día está validado (Regla 3, sección 3.3 del spec) para aguantar rangos multi-año en **una sola llamada por indicador**, sin necesidad de paginar por mes como sí hace falta en ESIOS con indicadores horarios/de mayor frecuencia. `fetch_range`/`run_backfill` reutilizan `build_tasks()` (las mismas 3+3 tareas del job diario) tal cual, sin duplicar nada.
- **`ingestion_date` de las filas del backfill = el día en que se ejecutó el backfill (hoy), no la fecha propia de cada registro** — mismo criterio que ya se usó en `to_bronze_rows`/`historical_backfill.py` de ESIOS: Bronze se particiona por fecha de ingesta según el spec, no por fecha del dato.
- Ejecutado de verdad contra la API real: mismo rango que ESIOS (2026-02-06 a 2026-08-05, sin necesidad de token ya que REData no requiere autenticación). Bronze REData pasó de 75 a **9.050 filas**.
- **Resultado: cobertura de `% penetración renovable` y `renewable_share` pasó de 48/4.320 horas (1,1%) a 4.321/4.321 (100%)**, verificado end-to-end reconstruyendo toda la cadena (Silver REData → KPI → feature de ML) desde cero con los datos nuevos.

## 2026-08-05 — validations.py: motor de reglas extraído de bronze_to_silver.py

- **Por qué no Great Expectations:** el propio spec da permiso explícito para saltárselo si da fricción ("evaluar; si da fricción usar expectativas nativas de DLT"). GE es un framework completo aparte (expectation suites, contexto de datos, checkpoints, config JSON/YAML) para conseguir lo mismo que ya hacíamos con ~15 líneas de expresiones nativas de Spark — justo el riesgo de "colección de tecnologías" que los tutores señalaron en la sección 2. Como todo el pipeline ya vive dentro de Spark/Delta de principio a fin, las expresiones nativas se integran en el mismo DataFrame/plan de ejecución sin añadir un paso de validación paralelo con su propia dependencia.
- **Por qué no `@dlt.expect` (todavía):** a diferencia de GE, esto no es "elegimos no usarlo" — literalmente no se puede ejecutar en local, `@dlt.expect` solo existe dentro de una pipeline real de Delta Live Tables corriendo en un workspace de Databricks. Todo el pipeline se ha construido y verificado en local a propósito (así se encontraron el bug de zona horaria, se verificó el dedup con datos inyectados, etc.) — atar la validación a `@dlt.expect` desde ahora habría impedido probar nada de esto sin gastar crédito de Azure en cada iteración. No es abandonar DLT: la lógica de reglas se ha escrito de forma agnóstica al runtime (funciones que devuelven flags a partir de un DataFrame), lista para envolverse en `@dlt.table`/`@dlt.expect` cuando se migre a Databricks, sin rediseñar nada.
- **Extraído de `bronze_to_silver.py` a `seip/quality/validations.py`** — las reglas (rango de precio, solar nocturna) estaban ya construidas y verificadas (PR de Silver ESIOS), pero enterradas como lógica inline específica de ESIOS. Se generalizó a un motor pequeño (`Rule`: nombre + condición Python + condición Spark equivalente, `apply_rules`/`rules_flags_column` para evaluarlas) para que sea reutilizable — p.ej. por reglas de REData en el futuro, que hoy no tiene ninguna — en vez de tener que repetir el patrón a mano cada vez.
- **Cero cambios de comportamiento, solo reorganización:** los 82 tests existentes (incluidos los de `bronze_to_silver.py` que ejercitan `validate_esios_record`/`add_esios_validation_flags` como API pública sin cambios) siguen en verde tal cual, sin tocarlos — confirma que el refactor no alteró ningún resultado. Reverificado además end-to-end contra datos reales (125.255 filas), mismos flags de `night_solar_generation_above_threshold` que antes del refactor.

## 2026-08-05 — Soak test de kafka_producer.py (corto pero representativo, no las 48h literales)

- **Decisión consciente de no hacer las 48h del criterio de aceptación literal del spec ahora** — se aplazan a cuando el producer esté desplegado de verdad contra Confluent Cloud/Databricks, que es donde tiene sentido dejar algo corriendo días sin vigilancia. En su lugar, una prueba corta pero que comprueba las mismas propiedades de resiliencia de forma proporcional: el proceso sigue vivo tras muchos ciclos, la memoria no crece, y los 4 indicadores respetan su cadencia nativa sin atascarse.
- **Ejecutado de verdad, `run_forever()` contra Kafka local + ESIOS real, sin intervención manual, durante ~3h25** (más de las 2h planeadas — el tiempo real transcurrido en la sesión superó el objetivo, lo cual solo hace la prueba más representativa). Resultado:
  - **100 ciclos de publicación completados**, cadencia correcta y consistente en los 4 indicadores (PVPC cada hora, SPOT cada 15min, eólica/solar cada 5min)
  - **Memoria estable**: 34,9 MB al arrancar → 35,8 MB al parar — variación mínima, sin tendencia de crecimiento sostenido (lo que indicaría una fuga)
  - **Cero errores ni tracebacks** en todo el log
  - Bronze (`data/bronze/esios`) creció de forma continua durante toda la prueba (~+500 filas, consistente con los 100 ciclos), confirmando que los datos llegaban sin huecos
- **Pendiente real, anotado explícitamente:** las 48h completas, y contra Confluent Cloud (no Docker local) — ambas cosas solo tienen sentido una vez desplegado en la infraestructura real, parte de la migración a Azure/Databricks ya anotada en TODO.md.

## 2026-08-05 — Wheel verificado en entorno limpio (criterio de aceptación Fase 5)

- Distinción importante hecha explícita con el usuario antes de empezar: el criterio de aceptación pide instalar el wheel en un **entorno Python limpio**, no necesariamente en Databricks — subirlo como librería a un cluster real es un paso aparte, parte de la migración a Azure ya aparcada, no de esta verificación.
- `python -m build --wheel` genera `seip-0.1.0-py3-none-any.whl` con los 4 subpaquetes (`ingestion`/`quality`/`transform`/`ml`) incluidos correctamente.
- Instalado en un venv nuevo **fuera del proyecto** (sin ninguno de los extras `dev`/`spark`/`ml`/`streaming`) — solo arrastra `requests` como dependencia. Confirma en la práctica el diseño desde el scaffolding inicial: la lógica de negocio pura se puede instalar y usar sin PySpark/MLflow/confluent-kafka.
- Verificado que los módulos representativos de los 4 subpaquetes importan y ejecutan de verdad en ese entorno limpio (`kafka_producer.INDICATORS`, `quality.validations.ESIOS_VALIDATION_RULES`, `ml.train.registered_model_name`, etc.), no solo que el `pip install` no lanzara error.

## 2026-08-05 — Empieza la migración a Azure: storage ADLS Gen2 creado

- **Decisión de flujo de trabajo:** Peru crea los recursos de Azure/Databricks a mano en el portal (control directo sobre su propia cuenta), y me reporta los nombres/estructura reales para que yo adapte el código en consecuencia — no aprovisiono nada por CLI ni automatizado.
- Storage account real creado: **resource group `rg-seip`, storage account `seipdatalake`** (ADLS Gen2, hierarchical namespace activado), contenedores `bronze`/`silver`/`gold` — mapea 1:1 con las tres capas del Medallion.
- Rutas ABFSS resultantes (para cuando se conecte el acceso desde Databricks):
  - `abfss://bronze@seipdatalake.dfs.core.windows.net/`
  - `abfss://silver@seipdatalake.dfs.core.windows.net/`
  - `abfss://gold@seipdatalake.dfs.core.windows.net/`
- Todavía sin cambios en el código: las funciones ya reciben la ruta como parámetro (`bronze_path`, `silver_path`, etc.), así que no hace falta tocar nada hasta que el acceso Databricks→storage esté configurado y se pueda probar de verdad — cambiar las rutas sin poder verificarlas sería documentar algo no probado.
- **Workspace de Databricks creado: `dbw-seip`, región Switzerland North** — misma región que `seipdatalake`, confirmado explícitamente antes de seguir para evitar coste/latencia entre regiones. Pricing Trial (Premium, 14 días de DBU gratis — pasado ese plazo el workspace sigue funcionando igual, solo empieza a facturar la parte de licencia DBU además del cómputo/storage de Azure que ya se cobra desde el día 1). Workspace type **Hybrid** (no Serverless): necesitamos cluster clásico personalizable para poder instalar el paquete Maven de Kafka y el wheel propio como librerías, que es el camino ya probado, frente a Serverless que es más nuevo y con menos certeza de soportar esto igual de bien.
- **Decisión Unity Catalog vs Hive Metastore: Unity Catalog, sin fricción.** El workspace Premium nuevo lo trae activado por defecto — catálogo `dbw_seip` visible desde el primer momento, sin pasos manuales de habilitación. No hace falta el fallback a Hive Metastore que el spec dejaba como opción.
- **Storage conectado a Unity Catalog**: Access Connector `ac-seip` (managed identity, rol Storage Blob Data Contributor sobre `seipdatalake`) → Storage Credential `cred-seip` → 3 External Locations (`ext-bronze`/`ext-silver`/`ext-gold`), test de conexión en verde en las tres (Read/Write/List/Delete/Path Exists). El único fallo del test ("File Events") es una función opcional de Auto Loader que no usamos — no bloquea nada.
- **Cluster: DBR 15.4 LTS (Spark 3.5.x), no la 17.3 LTS que ofrecía por defecto (Spark 4.0.0).** Decisión deliberada: todo el proyecto está construido y verificado contra Spark 3.5.x en local (`pyproject.toml` fija `pyspark>=3.5,<3.6`) — Spark 4.0 es un salto de versión mayor sin ninguna prueba nuestra encima, y dado el historial de sorpresas de comportamiento sutil en este proyecto (el bug de zona horaria de `.collect()`), no tenía sentido arriesgarse a descubrir otra en un cluster de pago sin necesidad. Photon desactivado — acelerador de pago pensado para volúmenes grandes, sin beneficio perceptible a la escala de este proyecto (miles-cientos de miles de filas).
- **Secretos vía Azure Key Vault en vez de Databricks CLI**: se descartó el flujo estándar de `databricks secrets create-scope` (requiere instalar y autenticar la CLI de Databricks) a favor de un Key Vault (`kv-seip`) enlazado como secret scope vía la URL especial `#secrets/createScope` — todo hecho desde el portal/navegador, coherente con que Peru gestiona los recursos de Azure a mano. Encontrado y resuelto un permiso RBAC no obvio: ser Owner de la suscripción no concede automáticamente acceso a los *datos* de un Key Vault en modo de autorización RBAC (a diferencia del modelo clásico de "access policies") — hubo que asignarse explícitamente el rol "Key Vault Administrator" en el propio Key Vault y esperar la propagación (2-3 min) antes de poder crear el secreto.
- **Segundo permiso RBAC no obvio, distinto del anterior**: dar acceso al *usuario* (Peru) en el Key Vault no basta para que `dbutils.secrets.get(...)` funcione — es **Databricks como servicio** el que necesita leer el secreto, a través de la aplicación de primera parte `AzureDatabricks` (ya existente en el directorio, no la crea nadie). Hubo que asignarle el rol **Key Vault Secrets User** explícitamente en `kv-seip`, aparte del permiso del propio usuario.

## 2026-08-20 — Primera prueba real end-to-end en Databricks: funciona

Notebook `00_smoke_test_azure` en `cluster-seip-dev`, tres celdas:
1. `dbutils.secrets.get(scope="seip-secrets", key="esios-api-token")` → token cargado correctamente
2. `seip.ingestion.esios_client.get_indicator_metadata(1001, ...)` — **llamada real a la API de ESIOS desde el wheel instalado en el cluster**, respuesta correcta (y esta vez el `€` se renderiza bien, al no depender de la consola de Windows como en local)
3. `spark.createDataFrame(...).write.format("delta").save("abfss://bronze@seipdatalake.dfs.core.windows.net/smoke_test")` + lectura de vuelta — **escritura y lectura real en ADLS Gen2 vía Unity Catalog**, sin errores

Confirma la cadena completa conectada de verdad: cluster (DBR 15.4/Spark 3.5.x) + wheel + Databricks Secrets (Key Vault) + Unity Catalog + ADLS Gen2. Nota de diseño: en el notebook se usa la variable `spark` ya existente del runtime de Databricks, no `seip.ingestion.spark_session.build_local_spark_session()` (esa es solo para desarrollo local, donde no hay un SparkSession ya creado) — las funciones `run(...)` de cada módulo ya reciben `spark` como parámetro, así que no hace falta ningún cambio de código para este paso, solo pasarle el `spark` del notebook en vez de crear uno nuevo.

**Siguiente prueba, ya con código real del pipeline (no celdas sueltas):** `seip.ingestion.batch_job.run(spark, target_date, bronze_path="abfss://bronze@seipdatalake.dfs.core.windows.net/redata")`, sin ningún cambio respecto a la versión probada en local. Resultado: `intercambios_francia/portugal/marruecos` fallaron como no críticos y se saltaron correctamente (reproduce en la nube exactamente el comportamiento ya validado en local con ese endpoint inestable), y los datos de `generacion_estructura` aterrizaron correctamente en el Bronze real de Azure con el esquema esperado (`ingestion_date`/`source`/`fetched_at`/`raw_json`).

**`bronze_to_silver.run_redata()` contra ese Bronze real**, también sin cambios: columnas tipadas, UTC normalizado, `percentage` presente — idéntico a lo verificado en local.

**Rodeo para desbloquear la cadena de ESIOS sin resolver todavía Kafka/Confluent Cloud:** `historical_backfill.py` no depende de Kafka (llama a la API de ESIOS directamente), así que sirve para meter datos reales de ESIOS en el Bronze de Azure y seguir probando Silver/join/Gold/features sin bloquearse en las dos decisiones pendientes de streaming. Ejecutado para 14 días (2026-08-06 a 2026-08-20): **9.748 filas**, coherente con la mezcla de granularidades esperada (PVPC hora + SPOT 15min + eólica/solar 5min).

**`bronze_to_silver.run_esios()` contra ese Bronze real** (streaming con checkpoint en `abfss://silver@.../_checkpoints/esios_silver/` — carpeta de metadata de Spark dentro del propio contenedor `silver`): tipado, `validation_flags` correcto, sin cambios de código.
